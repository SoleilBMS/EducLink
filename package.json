{
  "name": "educlink",
  "version": "0.1.0",
  "private": true,
  "description": "EducLink monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "npm run lint && npm run test",
    "lint": "node --check packages/core/src/tenantScope.js",
    "test": "node --test packages/core/src/tenantScope.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
